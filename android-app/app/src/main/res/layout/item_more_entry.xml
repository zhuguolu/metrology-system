<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="116dp"
    android:background="@drawable/bg_more_card"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingLeft="8dp"
    android:paddingTop="10dp"
    android:paddingRight="8dp"
    android:paddingBottom="10dp"
    android:stateListAnimator="@null">

    <FrameLayout
        android:id="@+id/moreCardIconChip"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/bg_more_icon_chip_blue"
        android:padding="1dp">

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_more_icon_chip_gloss" />

        <ImageView
            android:id="@+id/moreCardIconLight"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:alpha="0.26"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_nav_dashboard"
            android:tint="@android:color/white" />

        <ImageView
            android:id="@+id/moreCardIcon"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_gravity="center"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_nav_dashboard"
            android:tint="@color/navActive" />
    </FrameLayout>

    <TextView
        android:id="@+id/moreCardLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="9dp"
        android:ellipsize="end"
        android:gravity="center"
        android:letterSpacing="0.02"
        android:maxLines="2"
        android:minLines="2"
        android:textColor="@color/moreCardText"
        android:textSize="11sp"
        android:textStyle="bold" />

</LinearLayout>
